import { PageLoading } from "@/components/page-loading";

export default function AccountLoading() {
  return (
    <PageLoading
      eyebrow="Loading your account"
      intro="Guiding Light is preparing your sign-in, profile, or admin tools so the next step stays clear."
      title="Getting everything ready."
    />
  );
}
